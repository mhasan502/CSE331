ORG 100H

LEA SI, ARRAY
MOV CX, 5

MOV [SI], 1
MOV AL, [SI]
MOV DL, AL
INC SI
SUB CX, 1

FUNCTION:
MOV AL, [SI-1]
ADD AL, 2
MOV [SI], AL
ADD DL, [SI]
INC SI
LOOP FUNCTION

MOV SUM, DL

RET

ARRAY DB 10 DUP(0)
SUM DB 0